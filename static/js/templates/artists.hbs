<div class="row">
  <div class="col-md-4">
    <div id="artist-list" class="list-group">
      {{#each}}
        {{#link-to 'artist' this class="list-group-item"}}
        {{name}}
        {{/link-to}}
      {{/each}}
    </div>
  </div>

  <div class="col-md-8">
    {{outlet}}
  </div>
</div>
